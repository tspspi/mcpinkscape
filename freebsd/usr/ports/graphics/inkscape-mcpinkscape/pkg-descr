Inkscape SVG editor with an ABI-matched native mcpInkscape bridge.

The editor and bridge are built together using the graphics/inkscape recipe.
The bridge provides explicit Start/Stop/Status actions and local, per-user
live document editing for the separately installed mcpInkscape Python server.
